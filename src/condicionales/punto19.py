llueve = False

if not llueve:
    print("Podemos salir al parque.")
   
   # Este programa utiliza una variable booleana llamada `llueve` para indicar si está lloviendo o no. Luego, emplea la estructura condicional `if` junto con el operador lógico `not` para verificar si **no** está lloviendo. Si la condición se cumple, imprime el mensaje "Podemos salir al parque".