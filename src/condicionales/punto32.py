dividendo = 10
divisor = 2
resultado = dividendo / divisor if divisor != 0 else "División por cero no permitida"
print(resultado)

# Este programa utiliza una expresión condicional (operador ternario) para realizar una división solo si el divisor es diferente de cero, evitando el error de división por cero.