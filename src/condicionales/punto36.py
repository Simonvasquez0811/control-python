numeros = [0, 0, 1, 0]

if any(numeros):
    print("Al menos un número es no cero.")

    #Este programa verifica si al menos uno de los elementos de una lista es diferente de cero utilizando la función `any()`.
