entrada = ""
while not entrada.isdigit():
    entrada = input("Introduce un número: ")

print(f"Has introducido el número: {entrada}")
# Este código solicita al usuario que introduzca un número y valida la entrada.